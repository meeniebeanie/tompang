<div class="panel panel-default">
  <div class="panel-body">
    <%= simple_form_for @product do |f| %>


      <% if product.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

          <ul>
          <% product.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>


      <%= f.input :product_name, placeholder: "What are you selling?" %>
      <%= f.input :country, include_blank: false, priority: [ "Singapore" ] %>
      <%= f.input :product_description, placeholder: "What's cool about it?" %>
      <%= f.input :price, placeholder: "How much is it?"%>
      <%= f.submit %>
    <% end %>
  </div>
</div>
